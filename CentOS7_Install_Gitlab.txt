0. Install and configure the necessary dependencies
 - sudo yum install -y curl policycoreutils-python openssh-server 
 - sudo systemctl enable sshd 
 - sudo systemctl start sshd 
 - sudo firewall-cmd --permanent --add-service=http 
 - sudo systemctl reload firewalld
 
 - sudo yum install postfix
 - sudo systemctl enable postfix
 - sudo systemctl start postfix
 
1. yum 패키지 정보를 다운로드
 - #curl https://packages.gitlab.com/install/repositories/gitlab/gitlab-ce/script.rpm.sh | sudo bash
2. GitLab 설치
 - #EXTERNAL_URL="http://192.168.1.53:9000" yum install -y gitlab-ce
 - 포트 변경 예정이면 포트번호까지 지정해줌
3. 서비스 포트 변경 (선택사항)
 - # vi /etc/gitlab/gitlab.rb
   . nginx['listen_port'] = nil -> 9000
   . unicorn['port'] = 8080 -> 9001
 - # gitlab-ctl reconfigure

4. 웹브라우저 접속
 - id : root pw 설정 : smcs2019


5. smtp 설정
   . sudo -e /etc/gitlab/gitlab.rb
   . gitlab_rails['smtp_enable'] = true
   . gitlab_rails['smtp_address'] = "smtp.gmail.com"
   . gitlab_rails['smtp_port'] = 587
   . gitlab_rails['smtp_user_name'] = "sang.min.lee40@gmail.com"
   . gitlab_rails['smtp_password'] = "my-gmail-password"
   . gitlab_rails['smtp_domain'] = "smtp.gmail.com"
   . gitlab_rails['smtp_authentication'] = "login"
   . gitlab_rails['smtp_enable_starttls_auto'] = true
   . gitlab_rails['smtp_tls'] = false
   . gitlab_rails['smtp_openssl_verify_mode'] = 'peer'

   . gitlab_rails['time_zone'] = 'Asia/Seoul'
   . gitlab_rails['time_enable'] = true

   . sudo gitlab-ctl reconfigure
   //. sudo gitlab-ctl restart
   . sudo gitlab-ctl status

